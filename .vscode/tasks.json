{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run test",
            "type": "shell",
            "command": "python3",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "args": [
                "./test.py"
            ],
            "group": "build",
        },
        {
            "label": "run train",
            "type": "shell",
            "command": "python3",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "args": [
                "./train.py"
            ],
            "group": "build",
        }
    ]
}